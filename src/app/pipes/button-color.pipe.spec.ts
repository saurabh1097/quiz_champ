import { ButtonColorPipe } from './button-color.pipe';

describe('ButtonColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonColorPipe();
    expect(pipe).toBeTruthy();
  });
});
